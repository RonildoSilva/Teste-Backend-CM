/*
const {Pool} = require('pg')

const psqlConfiguration = {
    host: 'localhost',
    user: 'postgres',
    password: 'postgres',
    port: '5432',
    database: 'company_control_api'
}

let pool = new Pool(psqlConfiguration);

module.exports = pool;
*/